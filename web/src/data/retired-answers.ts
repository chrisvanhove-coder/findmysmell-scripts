/**
 * Ответы, которые есть в таблице весов, но в квизе не участвуют.
 *
 * Таблица весов (`answer-weights.json`) — точная выгрузка из прода, её
 * не правим, иначе сверка с продовым движком перестанет что-либо доказывать.
 * Вместо этого перечисляем выведенные из оборота коды здесь: так видно,
 * что это решение, а не потерянная кнопка.
 */
export const RETIRED_ANSWERS: Record<string, string> = {
  // Скрыты в Webflow: выборка была слишком большой.
  Q_DAYTDAY__NOTKNOWN: 'скрыт — слишком много вариантов на вопросе',
  Q_CELEBRATE__FLOWERS: 'скрыт — слишком много вариантов на вопросе',
  Q_CELEBRATE__INCENSE: 'скрыт — слишком много вариантов на вопросе',
  Q_CELEBRATE__LEMONAD: 'скрыт — слишком много вариантов на вопросе',
  Q_CELEBRATE__TROPICS: 'скрыт — слишком много вариантов на вопросе',

  // Кнопки удалены со страниц, варианты больше не нужны.
  Q_ENERGY__COFFEE: 'удалён из квиза',
  Q_ENV_CHILD__MIXED: 'удалён из квиза',
};

export function isRetired(code: string): boolean {
  return code in RETIRED_ANSWERS;
}
