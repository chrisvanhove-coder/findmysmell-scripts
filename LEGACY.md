# ⚠️ Файлы в корне сейчас работают в проде

`result48.js`, `result-shared15.js`, `result-fr6.js`, `result-shared-fr.js`
отдаются на живой сайт напрямую через jsDelivr с ветки `main`:

```
https://cdn.jsdelivr.net/gh/chrisvanhove-coder/findmysmell-scripts@main/result48.js
```

Версия не зафиксирована. Поэтому **не переименовывать, не перемещать и не
удалять их**, пока сайт не переехал на Next.js: любое изменение пути
мгновенно ломает страницу результата на findmysmell.com.

Новое приложение живёт в `web/` и на эти файлы не влияет.
Они остаются здесь до переключения трафика, после чего их можно убрать
одним коммитом.
